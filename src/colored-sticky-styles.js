import styled from "styled-components";

export const NoColor = styled.div`
  word-wrap: break-word;
  position: absolute;
  padding: 0.5em 1em;
  // width: 20em;
  margin: 0.3em;
  cursor: move;
  font-size: 12px;
  text-align: center;
  overflow: none;
`;
